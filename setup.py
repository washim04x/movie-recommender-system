from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A recommendation system that suggests similar movies when you like one, using cosine similarity on movie data.',
    author='washim',
    license='',
)
